> **Note:** This is a template prepared from your own billing data. It is not legal advice.
<!-- FILE VIA: Google Cloud Console → Billing → Support → "Create case", category "Billing", subcategory "Dispute a charge". Keep the case in writing; decline phone-only resolution. -->

**Subject:** Fraudulent API charges from leaked API key — billing adjustment requested — {{currency_symbol}}{{disputed_amount}}

Dear Google Cloud Billing Support,

I am the account holder for billing account **{{billing_account_id}}** ({{company_name}}, {{victim_email}}). I am reporting unauthorized, fraudulent API usage and requesting a **billing adjustment** for {{currency_symbol}}{{disputed_amount}} ({{currency_code}}) charged between **{{incident_start}}** and **{{incident_end}}** on project(s) **{{project_ids}}**.

**This traffic was not generated by me or my application.** The evidence, prepared from my own billing export and monitoring data:

1. **Spike magnitude.** My established baseline is {{currency_symbol}}{{baseline_daily_spend}}/day (median over the prior 30 days). During the {{incident_hours}}-hour incident window, spend ran at **{{spike_multiplier}}× that baseline** as a daily rate, peaking at **{{currency_symbol}}{{peak_hourly_cost}} in a single hour**.
2. **Model variety.** The abusing traffic called **{{distinct_skus_count}} distinct SKUs** — {{model_variety_summary}}. No legitimate application of mine uses this spread of models.
3. **Impossible volumes.** {{impossible_volume_summary}}
4. **Attribution.** The traffic is attributable to the API key **{{abused_key_display_name}}**, which was compromised. Per-credential request metrics for the incident window are attached.
5. **Prompt mitigation.** I discovered the charges at {{discovery_datetime}} and acted immediately: {{mitigation_summary}} (completed {{mitigation_datetime}}).

Attached exhibits:
{{exhibit_list_summary}}

This is a documented, recurring abuse pattern affecting many GCP customers (leaked unrestricted API keys driven by automated abuse services). Google's own guidance acknowledges that new API keys are created unrestricted by default.

**I request:**
1. A full billing adjustment of {{currency_symbol}}{{disputed_amount}} as fraudulent usage;
2. Confirmation in writing that collection on this amount is suspended while the dispute is open;
3. A reference number for this dispute.

I am withholding payment of the disputed amount while this case is open, per standard dispute practice. Please confirm receipt.

Regards,
{{victim_name}}
{{company_name}} — {{victim_email}}
{{filing_date}}
